[build]
  base = "frontend"
  command = "npm ci --legacy-peer-deps && npx ng build --configuration=production"
  publish = "dist/voltcommerce/browser"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
